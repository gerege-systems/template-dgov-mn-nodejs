# Secrets — compose interpolation + backend config (both gitignored on purpose;
# .env.example / backend/.env.example are the tracked templates).
.env
.env.*
!.env.example
backend.env

# Go-ийн эх хувилбар. Порт ДУУССАН тул энэ хавтас устгагдсан — оруулга нь
# хамгаалалт болж үлдэнэ: хэн нэгэн тулгах зорилгоор түр clone хийвэл санамсаргүй
# commit болохгүй. Эх хувилбар: gerege-systems/template-dgov-mn.
.go-reference/

# MkDocs-ийн build гаралт + виртуал орчин — үүсгэгддэг тул repo-д хадгалахгүй
# (GitHub Pages workflow нь CI дээр шинээр build хийнэ).
docs-site/site/
docs-site/.venv/

# Node
node_modules/
dist/
*.tsbuildinfo

# Build / coverage artifacts
coverage/
.next/
out/

# OS / editor junk
.DS_Store
.idea/
.vscode/
*.log
